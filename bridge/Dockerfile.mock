FROM python:3.12-slim
RUN useradd --system --uid 10002 --create-home bridge
WORKDIR /app
COPY requirements-mock.txt ./
RUN pip install --no-cache-dir -r requirements-mock.txt
COPY bridge ./bridge
USER bridge
ENTRYPOINT ["python", "-m", "bridge.main", "--mode", "mock"]
